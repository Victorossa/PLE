@model IEnumerable<GeneradorCodigoAdm.Models.tecn_bloque_codigo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Nombre del Bloque
        </th>
        <th>
           Tecnologia Utilizada
        </th>
        

        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.NombreBloque)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.tecn_tecnologias.NombreTecnologia)
    </td>
    

    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.BloqueID }) |
        @Html.ActionLink("Details", "Details", new { id = item.BloqueID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.BloqueID })
    </td>
</tr>
}

</table>
